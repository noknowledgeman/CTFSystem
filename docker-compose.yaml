#TODO Handle ENVS
services:
  frontend:
    build: ./frontend/
    env_file: ./frontend/.env
    ports:
      - "${FRONTEND_PORT}:80"
  
  backend:
    build: ./backend/
    env_file: ./backend/.env
    ports:
      - "${BACKEND_PORT}:8000"