# Validation Service

# Setup

```bash
docker compose up -d
```

Go to http://localhost:8000/ to setup a ctf and an admin account. In this admin account you can go to the /settings page and generate a new access token. 

