name: "Path Traversal Intranet"
description: "Corporate intranet with a path traversal flaw in the page parameter."
flag: "CTF{s3cur3_y0ur_p4th5}"
difficulty: "medium"
verify:
  script: "verify.py"
  language: "python"
  timeout: 30
  dependencies:
    - requests
deployment:
  remote_dir: "/root/sample1"
services:
  - name: "web"
    protocol: "http"
    host: "192.168.122.100"
    port: 8080
    path: "/"
    expected_status: 200
validation_steps:
  - type: "container_running"
  - type: "service_check"
    service: "web"
  - type: "verify_script"
